---
const base = import.meta.env.BASE_URL;
---
<section class="events" data-events>
  <img class="events__bg" src={`${base}Images/events3.webp`} alt="" decoding="async" />
  <div class="events__dim"></div>

  <div class="events__layout">
    <div class="events__top">
      <div class="events__left">
        <div class="events__collage">
          <div class="events__photo events__photo--a"><img src={`${base}Images/events1.webp`} alt="XR Club event" decoding="async" /></div>
          <div class="events__photo events__photo--b"><img src={`${base}Images/events2.webp`} alt="XR Club event" decoding="async" /></div>
          <div class="events__photo events__photo--c"><img src={`${base}Images/events4.webp`} alt="XR Club event" decoding="async" /></div>
          <div class="events__photo events__photo--d"><img src={`${base}Images/events7.webp`} alt="XR Club event" decoding="async" /></div>
          <div class="events__photo events__photo--e"><img src={`${base}Images/events5.webp`} alt="XR Club event" decoding="async" /></div>
          <div class="events__photo events__photo--f"><img src={`${base}Images/events3.webp`} alt="XR Club event" decoding="async" /></div>
        </div>
      </div>

      <div class="events__right">
        <div class="events__title-wrap">
          <span class="events__title-outline">XR</span>
          <h2 class="events__title">Events</h2>
        </div>
        <img src={`${base}Images/kickoff.webp`} alt="Spring Kickoff" class="events__kickoff-img" decoding="async" />
        <p class="events__desc">The XR Club hosts several events throughout the year and often partners with major student organizations. Our flagship event is our project demo presentation, where teams showcase their work. We're also in the middle of organizing a hackathon in partnership with IXA Alliance.</p>
        <p class="events__desc">Past highlights include Mini Demo Day, TerpCon 2026, KAMECON 2026, and the UMD Gaming LAN. We also run workshops and occasional Beat Saber tournaments. <strong>Join our Discord to stay updated on upcoming events!</strong></p>
        <a href="https://discord.gg/mn8ZKTwXFQ" target="_blank" rel="noopener" class="events__discord">Discord</a>
      </div>
    </div>
  </div>
</section>

<style>
  .events {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    will-change: transform;
    contain: content;
  }

  .events::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 7.5rem;
    background: linear-gradient(rgba(0, 0, 0, 0.95), transparent);
    z-index: 3;
    pointer-events: none;
  }

  .events::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 7.5rem;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.95));
    z-index: 3;
    pointer-events: none;
  }

  .events__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .events__dim {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.7);
    z-index: 1;
  }

  .events__layout {
    position: relative;
    z-index: 4;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(3rem, 5vh, 8rem) var(--page-padding) clamp(4rem, 8vh, 12rem);
    box-sizing: border-box;
  }

  .events__top {
    display: flex;
    align-items: center;
    gap: 4vw;
    width: 100%;
    height: 100%;
  }

  .events__left {
    flex: none;
    width: 45vw;
    height: 100%;
    margin-left: 6vw;
  }

  .events__right {
    flex: 1;
    min-width: 0;
    max-width: 28vw;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .events__kickoff-img {
    width: 100%;
    max-width: 100%;
    height: auto;
    object-fit: contain;
    border-radius: 0.75rem;
    filter: drop-shadow(0 8px 24px rgba(0, 0, 0, 0.5));
    margin-bottom: 1.2rem;
  }

  .events__title-wrap {
    position: relative;
    margin-bottom: 2rem;
  }

  .events__title-outline {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: 'Inter', sans-serif;
    font-size: clamp(8rem, 14vw, 15rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    color: transparent;
    -webkit-text-stroke: 1.5px rgba(255, 255, 255, 0.08);
    pointer-events: none;
    user-select: none;
    white-space: nowrap;
  }

  .events__title {
    position: relative;
    font-family: 'Inter', sans-serif;
    font-size: clamp(4.5rem, 9vw, 8rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    color: #fff;
    line-height: 0.9;
    text-shadow: 0 6px 30px rgba(0, 0, 0, 0.8), 0 2px 10px rgba(0, 0, 0, 0.9);
  }

  .events__desc {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.9rem, 0.9vw, 1.4rem);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.75;
    margin-bottom: 0.6rem;
  }

  .events__desc:last-child {
    margin-bottom: 0;
  }

  .events__desc strong {
    color: #fff;
    font-weight: 600;
  }

  .events__discord {
    display: inline-block;
    margin-top: 1.5rem;
    align-self: flex-start;
    font-family: 'Inter', sans-serif;
    font-size: clamp(1rem, 1.2vw, 1.4rem);
    font-weight: 500;
    color: #fff;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.18);
    padding: 0.75rem 2.5rem;
    border-radius: 980px;
    transition: all 200ms ease;
    cursor: pointer;
    text-decoration: none;
  }

  .events__discord:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.35);
  }

  .events__collage {
    width: 100%;
    height: 100%;
    transform: scale(0.8);
    transform-origin: center center;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr 1fr;
    gap: clamp(0.4rem, 0.5vw, 0.6rem);
  }

  .events__photo {
    border-radius: 8px;
    overflow: hidden;
    min-height: 0;
  }

  .events__photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 400ms ease;
  }

  .events__photo:hover img {
    transform: scale(1.05);
  }

  .events__photo--a { grid-column: 1; grid-row: 1; }
  .events__photo--b { grid-column: 2; grid-row: 1; }
  .events__photo--c { grid-column: 1; grid-row: 2; }
  .events__photo--d { grid-column: 2; grid-row: 2; }
  .events__photo--e { grid-column: 1; grid-row: 3; }
  .events__photo--f { grid-column: 2; grid-row: 3; }

  @media (max-width: 1024px) {
    .events__left {
      width: 40vw;
      margin-left: 3vw;
    }

    .events__collage {
      transform: scale(0.85);
    }

    .events__desc {
      font-size: clamp(0.85rem, 1vw, 1.1rem);
    }

  }

  @media (max-width: 768px) {
    .events {
      height: auto;
      min-height: 100vh;
    }

    .events__layout {
      padding: 6vh var(--page-padding);
      overflow-y: auto;
    }

    .events__top {
      flex-direction: column;
      gap: 2vh;
    }

    .events__left {
      width: 100%;
      height: auto;
      margin-left: 0;
    }

    .events__discord {
      align-self: center;
    }

    .events__right {
      max-width: 100%;
      align-items: center;
      text-align: center;
    }


    .events__collage {
      transform: none;
      grid-template-columns: 1fr 1fr 1fr;
      grid-template-rows: 1fr 1fr;
    }

    .events__photo--a { grid-column: 1; grid-row: 1; }
    .events__photo--b { grid-column: 2; grid-row: 1; }
    .events__photo--c { grid-column: 3; grid-row: 1; }
    .events__photo--d { grid-column: 1; grid-row: 2; }
    .events__photo--e { grid-column: 2; grid-row: 2; }
    .events__photo--f { grid-column: 3; grid-row: 2; }

    .events__desc {
      font-size: clamp(0.85rem, 1.5vw, 1rem);
      line-height: 1.6;
    }

    .events__title-outline { font-size: 7rem; }
    .events__title { font-size: 3rem; }
  }

  @media (max-width: 480px) {
    .events__layout {
      padding: 4rem 1rem 3rem;
    }

    .events__collage {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 1fr 1fr;
    }

    .events__photo--a { grid-column: 1; grid-row: 1; }
    .events__photo--b { grid-column: 2; grid-row: 1; }
    .events__photo--c { grid-column: 1; grid-row: 2; }
    .events__photo--d { grid-column: 2; grid-row: 2; }
    .events__photo--e { display: none; }
    .events__photo--f { display: none; }

    .events__desc { font-size: 0.9rem; }

    .events__title-outline { font-size: 5rem; }
    .events__title { font-size: 2.5rem; }
  }
</style>
