---
const base = import.meta.env.BASE_URL;
---
<section class="about" data-about>
  <img class="about__bg-img" src={`${base}Images/image8.webp`} alt="" decoding="async" />
  <div class="about__dim"></div>

  <div class="about__content" data-about-content>
    <div class="about__left" data-about-header>
      <div class="about__title-wrap">
        <span class="about__title-outline">XR</span>
        <h2 class="about__title">About</h2>
      </div>
      <p class="about__intro">The XR Club at UMD is a social and academic community exploring extended reality technology. Members are welcome to come into our lab and make full use of our resources in a relaxed, open environment. We host workshops, info sessions, and community events throughout the year. At our core, we create opportunities for students of all backgrounds and experience levels, connecting them with corporate partners and professors to gain hands-on experience building beautiful, immersive experiences.</p>
    </div>

    <div class="about__right" data-about-grid>
      <div class="about__card about__card--text" data-about-card>
        <h3 class="about__card-title">20+ Active Projects</h3>
        <p class="about__card-text">We offer 20+ hands on XR development and research projects, many with corporate sponsors or faculty mentors. <strong>There's no competitive application process</strong>, we encourage freshmen and sophomores to apply! Projects are organized by level to support growth.</p>
      </div>

      <div class="about__card about__card--image" data-about-card>
        <img src={`${base}Images/photo2.webp`} alt="XR Club members" class="about__card-img" decoding="async" />
      </div>

      <div class="about__card about__card--image" data-about-card>
        <img src={`${base}Images/photo3.webp`} alt="XR Club project" class="about__card-img" decoding="async" />
      </div>

      <div class="about__card about__card--levels" data-about-card>
        <div class="about__level">
          <h4 class="about__level-name">Gateway</h4>
          <p class="about__level-desc">Gain hands on experience working in a team environment with the most freedom.</p>
        </div>
        <div class="about__level">
          <h4 class="about__level-name">Intermediate</h4>
          <p class="about__level-desc">Work on projects involving corporate sponsors with a bigger commitment.</p>
        </div>
        <div class="about__level">
          <h4 class="about__level-name">Advanced</h4>
          <p class="about__level-desc">Collaborate with professors on projects that include a research component.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    will-change: transform;
    contain: content;
  }

  .about::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(rgba(0, 0, 0, 0.95), transparent);
    z-index: 3;
    pointer-events: none;
  }

  .about::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.95));
    z-index: 3;
    pointer-events: none;
  }

  .about__bg-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .about__dim {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.75);
    z-index: 1;
  }

  .about__content {
    position: relative;
    z-index: 2;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(3rem, 5vh, 8rem) var(--page-padding) clamp(4rem, 8vh, 12rem);
    gap: 4vw;
  }

  .about__left {
    flex: none;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .about__title-wrap {
    position: relative;
    margin-bottom: 2rem;
  }

  .about__title-outline {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: 'Inter', sans-serif;
    font-size: clamp(8rem, 14vw, 15rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    color: transparent;
    -webkit-text-stroke: 1.5px rgba(255, 255, 255, 0.08);
    pointer-events: none;
    user-select: none;
    white-space: nowrap;
  }

  .about__title {
    position: relative;
    font-family: 'Inter', sans-serif;
    font-size: clamp(4.5rem, 9vw, 8rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    color: #fff;
    line-height: 0.9;
    text-shadow: 0 6px 30px rgba(0, 0, 0, 0.8), 0 2px 10px rgba(0, 0, 0, 0.9);
  }

  .about__intro {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.9rem, 0.9vw, 1.4rem);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.75;
    max-width: 28vw;
  }

  .about__right {
    flex: none;
    width: 45vw;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    gap: clamp(0.75rem, 1vw, 1.25rem);
  }

  .about__card {
    border-radius: 12px;
    overflow: hidden;
    min-height: 0;
  }

  .about__card--text {
    background: rgba(0, 0, 0, 0.55);
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: clamp(1rem, 1.5vw, 3rem);
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 100%;
    overflow-y: auto;
  }

  .about__card--image {
    position: relative;
  }

  .about__card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 12px;
  }

  .about__card-title {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.85rem, 1.1vw, 1.8rem);
    font-weight: 700;
    color: #fff;
    margin-bottom: 0.8rem;
    letter-spacing: -0.02em;
  }

  .about__card-text {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.7rem, 0.85vw, 1.3rem);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.65);
    line-height: 1.6;
    margin-bottom: 0.6rem;
  }

  .about__card-text:last-child {
    margin-bottom: 0;
  }

  .about__card-text strong {
    color: #fff;
    font-weight: 600;
  }

  .about__card--levels {
    background: rgba(0, 0, 0, 0.55);
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: clamp(1rem, 1.5vw, 3rem);
    height: 100%;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: clamp(0.75rem, 1vw, 1.5rem);
  }

  .about__level {
    padding-left: clamp(0.75rem, 0.8vw, 1.25rem);
    border-left: 2px solid rgba(255, 255, 255, 0.2);
  }

  .about__level-name {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.8rem, 0.95vw, 1.5rem);
    font-weight: 600;
    color: #fff;
    letter-spacing: -0.01em;
    margin-bottom: 0.2rem;
  }

  .about__level-desc {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.7rem, 0.8vw, 1.2rem);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.55);
    line-height: 1.5;
  }

  @media (max-width: 768px) {
    .about {
      height: auto;
      min-height: 100vh;
      overflow: visible;
      contain: layout style;
    }

    .about__content {
      flex-direction: column;
      padding: 6vh var(--page-padding);
      gap: 3vh;
      height: auto;
    }

    .about__left {
      align-items: center;
      text-align: center;
    }

    .about__intro {
      max-width: 100%;
      font-size: 0.95rem;
    }

    .about__right {
      width: 100%;
      height: auto;
      grid-template-columns: 1fr 1fr;
    }

    .about__card-title {
      font-size: 1.2rem;
    }

    .about__card-text {
      font-size: 0.95rem;
    }

    .about__level-name {
      font-size: 1.1rem;
    }

    .about__level-desc {
      font-size: 0.9rem;
    }

    .about__title-outline {
      font-size: 7rem;
    }

    .about__title {
      font-size: 3rem;
    }
  }

  @media (max-width: 480px) {
    .about__content {
      padding: 4rem 1rem 3rem;
    }

    .about__right {
      grid-template-columns: 1fr;
    }

    .about__card--text,
    .about__card--levels {
      padding: 1.2rem;
    }

    .about__card-title {
      font-size: 1.15rem;
    }

    .about__card-text {
      font-size: 0.9rem;
    }

    .about__level-name {
      font-size: 1.05rem;
    }

    .about__level-desc {
      font-size: 0.88rem;
    }

    .about__title-outline {
      font-size: 5rem;
    }

    .about__title {
      font-size: 2.5rem;
    }
  }
</style>
