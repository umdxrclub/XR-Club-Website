---
const base = import.meta.env.BASE_URL;
---
<section class="proj" data-projects>
  <img class="proj__bg" src={`${base}Images/XR CLUB.webp`} alt="" decoding="async" />
  <div class="proj__dim"></div>

  <div class="proj__layout">
    <div class="proj__content">
      <div class="proj__left">
        <div class="proj__title-wrap">
          <span class="proj__title-outline">XR</span>
          <h2 class="proj__title">XR Creators</h2>
        </div>
        <p class="proj__desc">XR Creators are at the core of our membership. They are engineers, designers, and risk-takers who refuse to settle for what they've built and push themselves to create something greater. <strong>Whether you're a freshman with no experience but a strong drive to learn, or a senior with extensive experience, you're welcome here.</strong></p>
        <p class="proj__desc proj__desc--spaced">As an XR Creator, you can propose your own projects and request the resources or equipment needed to bring them to life. Note that new project proposals may undergo a more rigorous review than applications to join existing projects.</p>
        <div class="proj__btns">
          <a href="#apply" class="proj__apply">Apply Now</a>
          <a href={`${base}ideate`} class="proj__apply proj__apply--outline">Project Proposals</a>
        </div>
      </div>

      <div class="proj__right">
        <div class="proj__card">
          <div class="proj__vid-wrap">
            <video class="proj__vid" src={`${base}demo.mp4`} preload="metadata" playsinline data-proj-video></video>
            <button class="proj__play" data-play-btn aria-label="Play/Pause">
              <svg class="proj__icon-play" viewBox="0 0 24 24" fill="currentColor" width="26" height="26"><polygon points="5,3 19,12 5,21"></polygon></svg>
              <svg class="proj__icon-pause" viewBox="0 0 24 24" fill="currentColor" width="22" height="22" style="display:none"><rect x="5" y="3" width="4" height="18"></rect><rect x="15" y="3" width="4" height="18"></rect></svg>
            </button>
            <button class="proj__fullscreen" data-fullscreen-btn aria-label="Fullscreen">
              <svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20"><path d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z"/></svg>
            </button>
            <p class="proj__caption">Explore a walkthrough of "Nyctophobia Simulator", created by Dinushka Jagodige '28, Ricky Jiang '29, Oliver Blevins '29, and Ronit Munshi '29 entirely from scratch using VIRNECT's exclusive software.</p>
          </div>
        </div>

        <div class="proj__card">
          <div class="proj__vid-wrap">
            <video class="proj__vid" src={`${base}testimony.MOV`} preload="metadata" playsinline data-proj-video></video>
            <button class="proj__play" data-play-btn aria-label="Play/Pause">
              <svg class="proj__icon-play" viewBox="0 0 24 24" fill="currentColor" width="26" height="26"><polygon points="5,3 19,12 5,21"></polygon></svg>
              <svg class="proj__icon-pause" viewBox="0 0 24 24" fill="currentColor" width="22" height="22" style="display:none"><rect x="5" y="3" width="4" height="18"></rect><rect x="15" y="3" width="4" height="18"></rect></svg>
            </button>
            <button class="proj__fullscreen" data-fullscreen-btn aria-label="Fullscreen">
              <svg viewBox="0 0 24 24" fill="currentColor" width="20" height="20"><path d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z"/></svg>
            </button>
            <p class="proj__caption">Ricky Jiang's testimony on working with VIRNECT with minimal prior coding experience.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.querySelectorAll('[data-play-btn]').forEach(btn => {
    const wrap = btn.closest('.proj__vid-wrap');
    const video = wrap?.querySelector('video') as HTMLVideoElement | null;
    if (!video) return;

    const playIcon = btn.querySelector('.proj__icon-play') as HTMLElement;
    const pauseIcon = btn.querySelector('.proj__icon-pause') as HTMLElement;

    btn.addEventListener('click', () => {
      if (video.paused) {
        video.play();
        playIcon.style.display = 'none';
        pauseIcon.style.display = 'block';
      } else {
        video.pause();
        playIcon.style.display = 'block';
        pauseIcon.style.display = 'none';
      }
    });

    video.addEventListener('ended', () => {
      playIcon.style.display = 'block';
      pauseIcon.style.display = 'none';
      video.currentTime = 0;
    });
  });

  document.querySelectorAll('[data-fullscreen-btn]').forEach(btn => {
    const wrap = btn.closest('.proj__vid-wrap');
    const video = wrap?.querySelector('video') as HTMLVideoElement | null;
    if (!video) return;

    btn.addEventListener('click', () => {
      if (video.requestFullscreen) {
        video.requestFullscreen();
      } else if ((video as any).webkitRequestFullscreen) {
        (video as any).webkitRequestFullscreen();
      }
    });
  });
</script>

<style>
  .proj {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    will-change: transform;
    contain: content;
  }

  .proj::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(rgba(0, 0, 0, 0.95), transparent);
    z-index: 3;
    pointer-events: none;
  }

  .proj::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.95));
    z-index: 3;
    pointer-events: none;
  }

  .proj__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .proj__dim {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.78);
    z-index: 1;
  }

  .proj__layout {
    position: relative;
    z-index: 4;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    padding: clamp(5rem, 8vh, 8rem) var(--page-padding) clamp(4rem, 6vh, 6rem);
    gap: 1.5vh;
  }

  .proj__content {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4vw;
    min-height: 0;
  }

  .proj__left {
    flex: none;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    max-width: 28vw;
  }

  .proj__title-wrap {
    position: relative;
    margin-bottom: 2rem;
  }

  .proj__title-outline {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: 'Inter', sans-serif;
    font-size: clamp(6rem, 11vw, 12rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    color: transparent;
    -webkit-text-stroke: 1.5px rgba(255, 255, 255, 0.08);
    pointer-events: none;
    user-select: none;
    white-space: nowrap;
  }

  .proj__title {
    position: relative;
    font-family: 'Inter', sans-serif;
    font-size: clamp(3.5rem, 7vw, 6.5rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    color: #fff;
    line-height: 0.9;
    text-shadow: 0 6px 30px rgba(0, 0, 0, 0.8), 0 2px 10px rgba(0, 0, 0, 0.9);
  }

  .proj__desc {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.9rem, 0.9vw, 1.4rem);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.75;
    margin-bottom: 0.15rem;
  }

  .proj__desc--spaced {
    margin-top: 0.6rem;
  }

  .proj__desc strong {
    color: #fff;
    font-weight: 600;
  }

  .proj__btns {
    display: flex;
    gap: 0.75rem;
    margin-top: 1.5rem;
    align-self: flex-start;
  }

  .proj__apply {
    display: inline-block;
    font-family: 'Inter', sans-serif;
    font-size: clamp(1rem, 1.2vw, 1.4rem);
    font-weight: 500;
    color: #fff;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.18);
    padding: 0.75rem 2.5rem;
    border-radius: 980px;
    transition: all 200ms ease;
    cursor: pointer;
    text-decoration: none;
  }

  .proj__apply:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.35);
  }

  .proj__apply--outline {
    background: transparent;
    border-color: rgba(255, 255, 255, 0.12);
    color: rgba(255, 255, 255, 0.7);
  }

  .proj__apply--outline:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.25);
    color: #fff;
  }

  .proj__right {
    flex: none;
    width: 45vw;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(0.75rem, 1vw, 1.25rem);
  }

  .proj__card {
    border-radius: 12px;
    overflow: hidden;
  }

  .proj__vid-wrap {
    position: relative;
    border-radius: 12px;
    overflow: hidden;
    width: 100%;
    aspect-ratio: 1;
  }

  .proj__vid {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .proj__play {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.55);
    border: 1.5px solid rgba(255, 255, 255, 0.3);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 3;
    opacity: 0;
    transition: opacity 250ms ease, background 200ms ease;
  }

  .proj__vid-wrap:hover .proj__play {
    opacity: 1;
  }

  .proj__play:hover {
    background: rgba(0, 0, 0, 0.75);
    border-color: rgba(255, 255, 255, 0.5);
  }

  .proj__fullscreen {
    position: absolute;
    bottom: 10px;
    right: 10px;
    width: 36px;
    height: 36px;
    border-radius: 8px;
    background: rgba(0, 0, 0, 0.55);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 4;
    opacity: 0;
    transition: opacity 250ms ease, background 200ms ease;
  }

  .proj__vid-wrap:hover .proj__fullscreen {
    opacity: 1;
  }

  .proj__fullscreen:hover {
    background: rgba(0, 0, 0, 0.8);
    border-color: rgba(255, 255, 255, 0.4);
  }

  .proj__caption {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.85rem, 1vw, 1.2rem);
    font-weight: 400;
    color: rgba(255, 255, 255, 0.95);
    line-height: 1.5;
    padding: 1.2rem 0.9rem 0.8rem;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.85));
    z-index: 2;
  }

  @media (max-width: 768px) {
    .proj__content {
      flex-direction: column;
      padding: 0;
      gap: 2vh;
      overflow-y: auto;
    }

    .proj__left {
      align-items: center;
      text-align: center;
      max-width: 100%;
    }

    .proj__right {
      display: none;
    }

    .proj__desc {
      font-size: 1.05rem;
    }

    .proj__btns {
      align-self: center;
      flex-direction: column;
      gap: 0.5rem;
    }

    .proj__title-outline { font-size: 5rem; }
    .proj__title { font-size: 2.5rem; }
  }

  @media (max-width: 480px) {
    .proj__layout { padding: 4rem 1rem 3rem; }

    .proj__desc {
      font-size: 1rem;
    }

    .proj__title-outline { font-size: 4rem; }
    .proj__title { font-size: 2rem; }
  }
</style>
