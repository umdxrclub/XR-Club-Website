---
// Each dashboard section has its own address, e.g. /suits/team/documents
import SuitsTeam from '../../../components/SuitsTeam.astro';

export function getStaticPaths() {
  return ['overview', 'proposal', 'tasks', 'meetings', 'documents', 'team'].map(view => ({ params: { view } }));
}
---

<SuitsTeam />
